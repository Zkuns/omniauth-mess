require 'spec_helper'

describe Omniauth::Mess do
  it 'has a version number' do
    expect(Omniauth::Mess::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
